For basic info, read: https://www.mintlify.com/docs/quickstart

download mintlify with `npm i -g mint`

start preview with `mintlify dev`

update spell correction dictionary by adding words one per line into `/vale/config/vocabularies/Radar/accept.txt`, lines be regex